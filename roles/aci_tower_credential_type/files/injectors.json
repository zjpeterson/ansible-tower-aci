{
    "env": {
        "ACI_HOST": "{{ host }}",
        "ACI_VALIDATE_CERTS": "{{ validate_certs }}",
        "ACI_USERNAME": "{{ username }}",
        "ACI_PASSWORD": "{{ password }}",
        "ACI_PRIVATE_KEY": "{{ private_key }}"
    },
    "extra_vars": {
        "aci_host": "{{ host }}",
        "aci_validate_certs": "{{ validate_certs }}",
        "aci_username": "{{ username }}",
        "aci_password": "{{ password }}",
        "aci_private_key": "{{ private_key }}"
    }
}
